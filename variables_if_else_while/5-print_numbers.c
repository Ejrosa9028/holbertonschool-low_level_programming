#include <stdio.h>

/**
 * main - Generates a random number and prints whether it is positive,
 *        negative, or zero.
 *
 * Return: 0 on success
 */
int main(void)
{
	for (int i = 0; i < 11; i++)
	{
		printf("%d", i);
	}

	printf("\n");

	return (0);
}
