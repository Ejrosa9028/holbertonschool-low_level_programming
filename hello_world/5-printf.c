#include <stdio.h>

/**
 * main - Prints exactly "a piece of art,"
 *        followed by a new line.
 *
 * Return: 0 on success
 */

int main(void) /* Declaro la funcion main*/
{
/*uso el comando printf para imprimir*/
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
