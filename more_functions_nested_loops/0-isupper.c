#include "main.h"
/**
 * _isupper - Imprime si es uppercase o lowercase
 * @c: recoge el numero del resultado
 *
 * Return: El valor del ultimo digito
 */
int _isupper(int c)
{
	if (c <= 122 && c >= 97)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
